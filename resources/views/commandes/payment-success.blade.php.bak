@extends('layouts.app')

@section('content')

<div class="min-h-screen bg-[#f7f7f5] flex items-center justify-center p-4 animate-fadeIn">
    <div class="w-full max-w-2xl">

        <!-- Main Success Card - Animated -->
        <div class="bg-white rounded-2xl shadow-2xl overflow-hidden animate-slideUp">

            <!-- Success Header -->
            <div class="bg-[#0a0a0a] text-white px-6 sm:px-8 py-12 sm:py-16 text-center relative overflow-hidden">
                <!-- Background accent -->
                <div class="absolute inset-0 opacity-5">
                    <div class="absolute top-0 right-0 w-40 h-40 bg-white rounded-full -translate-y-1/2 translate-x-1/2"></div>
                </div>

                <!-- Checkmark Icon -->
                <div class="mx-auto w-20 h-20 bg-white/10 rounded-full flex items-center justify-center mb-6 relative z-10 animate-scaleIn">
                    <svg class="w-10 h-10 text-white animate-drawCheckmark" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24" stroke-linecap="round" stroke-linejoin="round">
                        <polyline points="20 6 9 17 4 12"></polyline>
                    </svg>
                </div>

                <!-- Title -->
                <h1 class="font-serif text-[32px] sm:text-[40px] leading-tight mb-2 text-white relative z-10">Commande confirmée!</h1>
                <p class="text-[13px] sm:text-[14px] text-white/70 font-light relative z-10">Merci pour votre confiance</p>
            </div>

            <!-- Content -->
            <div class="p-6 sm:p-8 space-y-6">

                <!-- Main Info Box -->
                <div class="border border-[#e0e0dc] rounded-xl p-6 space-y-4 animate-fadeInUp" style="animation-delay: 150ms;">
                    <div class="flex items-start justify-between">
                        <div>
                            <div class="text-[11px] font-medium tracking-[0.08em] uppercase text-[#a0a09a] mb-1">Numéro de commande</div>
                            <div class="text-[22px] font-mono font-medium text-[#0a0a0a]">#{{ $commande->id }}</div>
                        </div>
                        <div class="text-right">
                            <div class="text-[11px] font-medium tracking-[0.08em] uppercase text-[#a0a09a] mb-1">Montant payé</div>
                            <div class="text-[24px] font-mono font-medium text-[#0a0a0a]">
                                {{ number_format($commande->total, 0, ',', ' ') }} FCFA
                            </div>
                        </div>
                    </div>

                    <!-- Date -->
                    <div class="pt-4 border-t border-[#efefed]">
                        <p class="text-[12px] text-[#a0a09a] font-light">
                            Confirmé le {{ $commande->created_at->format('d') }} {{ $commande->created_at->translatedFormat('F') }} {{ $commande->created_at->format('Y') }} à {{ $commande->created_at->format('H:i') }}
                        </p>
                    </div>
                </div>

                <!-- Delivery Info -->
                <div class="border border-[#e0e0dc] rounded-xl p-5 animate-fadeInUp" style="animation-delay: 250ms;">
                    <div class="flex items-start gap-3 mb-4">
                        <svg class="w-5 h-5 text-[#a0a09a] flex-shrink-0 mt-0.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                            <path d="M12 1v22M1 12h22M3.22 3.22l15.56 15.56M20.78 3.22L5.22 18.78" stroke-linecap="round"/>
                            <path d="M12 5v14M5 12h14"/>
                        </svg>
                        <div class="flex-1">
                            <div class="text-[11px] font-medium tracking-[0.08em] uppercase text-[#a0a09a] mb-2">Adresse de livraison</div>
                            <p class="text-[14px] font-medium text-[#0a0a0a]">{{ $commande->adresse_detail }}</p>
                            @if($commande->pays)
                                <p class="text-[12px] text-[#666660] mt-1">{{ $commande->pays }}</p>
                            @endif
                            <p class="text-[13px] text-[#a0a09a] font-light mt-2">📞 {{ $commande->telephone_livraison }}</p>
                        </div>
                    </div>
                </div>

                <!-- Timeline Steps -->
                <div class="border border-[#e0e0dc] rounded-xl p-5 animate-fadeInUp" style="animation-delay: 350ms;">
                    <div class="text-[11px] font-medium tracking-[0.08em] uppercase text-[#0a0a0a] mb-5">Statut de votre commande</div>
                    
                    <div class="space-y-4">
                        <!-- Step 1 -->
                        <div class="flex gap-4 items-start">
                            <div class="flex flex-col items-center">
                                <div class="w-8 h-8 rounded-full bg-[#0a0a0a] text-white flex items-center justify-center text-[11px] font-medium">✓</div>
                                <div class="w-0.5 h-8 bg-[#e0e0dc] mt-2"></div>
                            </div>
                            <div class="pt-1">
                                <p class="text-[13px] font-medium text-[#0a0a0a]">Paiement confirmé</p>
                                <p class="text-[12px] text-[#a0a09a] font-light">Votre paiement a été traité avec succès</p>
                            </div>
                        </div>

                        <!-- Step 2 -->
                        <div class="flex gap-4 items-start">
                            <div class="flex flex-col items-center">
                                <div class="w-8 h-8 rounded-full bg-[#e0e0dc] text-[#0a0a0a] flex items-center justify-center text-[11px] font-medium animate-pulse">2</div>
                                <div class="w-0.5 h-8 bg-[#e0e0dc] mt-2"></div>
                            </div>
                            <div class="pt-1">
                                <p class="text-[13px] font-medium text-[#0a0a0a]">Traitement de la commande</p>
                                <p class="text-[12px] text-[#a0a09a] font-light">Préparation en cours par le vendeur</p>
                            </div>
                        </div>

                        <!-- Step 3 -->
                        <div class="flex gap-4 items-start">
                            <div class="flex flex-col items-center">
                                <div class="w-8 h-8 rounded-full bg-[#e0e0dc] text-[#0a0a0a] flex items-center justify-center text-[11px] font-medium">3</div>
                            </div>
                            <div class="pt-1">
                                <p class="text-[13px] font-medium text-[#0a0a0a]">Livraison</p>
                                <p class="text-[12px] text-[#a0a09a] font-light">Votre commande sera en route vers vous</p>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Buttons -->
                <div class="grid grid-cols-1 sm:grid-cols-2 gap-3 animate-fadeInUp" style="animation-delay: 450ms;">
                    <a href="{{ route('client.commandes') }}"
                       class="block text-center px-4 py-3 bg-[#0a0a0a] text-white text-[13px] font-medium rounded-lg hover:opacity-90 active:scale-95 transition-all">
                        Voir mes commandes
                    </a>
                    <a href="{{ route('accueil') }}"
                       class="block text-center px-4 py-3 border border-[#e0e0dc] text-[#0a0a0a] text-[13px] font-medium rounded-lg hover:bg-[#f7f7f5] active:scale-95 transition-all">
                        Continuer les achats
                    </a>
                </div>

            </div>

            <!-- Footer -->
            <div class="bg-[#f7f7f5] px-6 sm:px-8 py-4 text-center border-t border-[#efefed]">
                <div class="text-[11px] text-[#a0a09a] font-light">
                    📧 Un email de confirmation a été envoyé à<br>
                    <span class="font-medium text-[#0a0a0a]">{{ auth()->user()->email }}</span>
                </div>
            </div>
        </div>

        <!-- Support Link -->
        <div class="mt-8 text-center animate-fadeInUp" style="animation-delay: 550ms;">
            <a href="#" class="inline-flex items-center gap-1.5 text-[12px] text-[#0a0a0a] hover:text-[#a0a09a] transition-colors">
                <svg class="w-4 h-4" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                    <path d="M18.364 5.636l-3.536 3.536m0 5.656l3.536 3.536M9.172 9.172L5.636 5.636m3.536 9.192l-3.536 3.536M21 12a9 9 0 11-18 0 9 9 0 0118 0zm-5 0a4 4 0 11-8 0 4 4 0 018 0z"/>
                </svg>
                Besoin d'aide ?
            </a>
        </div>
    </div>
</div>

<style>
@keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
}

@keyframes slideUp {
    from {
        opacity: 0;
        transform: translateY(20px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

@keyframes fadeInUp {
    from {
        opacity: 0;
        transform: translateY(10px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

@keyframes scaleIn {
    from {
        opacity: 0;
        transform: scale(0.8);
    }
    to {
        opacity: 1;
        transform: scale(1);
    }
}

@keyframes drawCheckmark {
    0% {
        stroke-dasharray: 50;
        stroke-dashoffset: 50;
        opacity: 0;
    }
    50% {
        opacity: 1;
    }
    100% {
        stroke-dasharray: 50;
        stroke-dashoffset: 0;
        opacity: 1;
    }
}

.animate-fadeIn {
    animation: fadeIn 0.6s ease-out;
}

.animate-slideUp {
    animation: slideUp 0.5s ease-out;
}

.animate-fadeInUp {
    animation: fadeInUp 0.5s ease-out forwards;
    opacity: 0;
}

.animate-scaleIn {
    animation: scaleIn 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275) 0.2s forwards;
    opacity: 0;
}

.animate-drawCheckmark {
    animation: drawCheckmark 0.6s ease-out 0.3s forwards;
}

/* Loading state for buttons */
button:disabled, button[data-loading="true"] {
    opacity: 0.7;
    pointer-events: none;
}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
    // Afficher une animation de progression
    if (typeof NProgress !== 'undefined') {
        NProgress.done();
    }

    // Pulse effect on step 2
    const step2 = document.querySelector('[style*="animation-delay: 250ms"]')?.parentElement;
    if (step2) {
        setTimeout(() => {
            step2.classList.add('ring-2', 'ring-offset-2', 'ring-[#0a0a0a]');
        }, 1000);
    }
});
</script>

@endsection
